{
  "name": "gold-com-vc",
  "version": "0.1.0",
  "private": true,
  "description": "Static site for gold.com.vc (SOLOMON). All public copy lives in content/site-copy.md; npm run build regenerates the root HTML files via scripts/build-site.mjs.",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-site.mjs",
    "watch": "node scripts/build-site.mjs --watch"
  },
  "dependencies": {
    "js-yaml": "^4.1.0",
    "marked": "^14.1.3"
  }
}
