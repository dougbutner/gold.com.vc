# `/test-plan`

1. Change summary. 2. Risks (auth, money, migration, concurrency, external APIs). 3. Levels: unit; integration where boundaries matter; **small** e2e set for critical journeys. 4. Fixtures/data. 5. Regression commands / flaky notes. 6. Merge/deploy exit criteria.

Default: minimal plan; expand if asked.
